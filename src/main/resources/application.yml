server:
  port: 8081
  servlet:
    context-path: /springai
spring:
  ai:
    ollama:
      embedding:
        options:
          model: qwen
      chat:
        options:
          model: qwen
          temperature: 0.8