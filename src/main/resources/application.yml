server:
  port: 8081
  servlet:
    context-path: /springai
spring:
  ai:
    ollama:
      embedding:
        options:
          model: llama3
      chat:
        options:
          model: llama3
          temperature: 0.8