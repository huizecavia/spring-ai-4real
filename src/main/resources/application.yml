server:
  port: 8081
  servlet:
    context-path: /springai
spring:
  ai:
    ollama:
      chat:
        options:
          model: qwen2.5-coder:7b
          temperature: 0.5
          max-completion-tokens: 128